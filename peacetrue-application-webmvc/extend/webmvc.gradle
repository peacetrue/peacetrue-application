task generateTemplate(group: "$name") {
    doLast {
        exec {
            workingDir("$projectDir/")
            commandLine("generate-template.sh")
            ignoreExitValue(true)
        }
        exec {
            workingDir(System.getenv("workingDir") + "/peacetrue-template")

        }
    }
}
